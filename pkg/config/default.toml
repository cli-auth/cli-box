# cli-box server configuration

# Global env defaults — applied to all CLIs
[env]
EDITOR = "vim"
PAGER = "less"

# Per-CLI credential mounts
[cli.gh]
mounts = [
    { name = "gh", target = "~/.config/gh" },
]

[cli.aws]
mounts = [
    { name = "aws", target = "~/.aws" },
]

[cli.gcloud]
mounts = [
    { name = "gcloud", target = "~/.config/gcloud" },
]

[cli.kubectl]
mounts = [
    { name = "kubectl", target = "~/.kube" },
]
